spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:karflow}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    show-sql: true

  flyway:
    clean-disabled: false

karflow:
  jwt:
    secret: ZGV2LXNlY3JldC1rZXktZm9yLWthcmZsb3ctZGV2LW9ubHktbXVzdC1iZS1hdC1sZWFzdC0yNTYtYml0cy1sb25n
    cookie-secure: false
    cookie-domain: localhost

logging:
  level:
    ma.karflow: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
